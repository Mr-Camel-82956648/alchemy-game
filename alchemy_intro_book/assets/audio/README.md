将 intro 阶段的音频资源放在这里。

当前文件名：

- `intro-bgm.mp3`
- `ui-click.mp3`

对应配置：

- `src/config/appConfig.js -> audio.bgmSrc`
- `src/config/appConfig.js -> audio.clickSrc`

说明：

- `intro-bgm.mp3` 是引导阶段背景音乐
- `ui-click.mp3` 是按钮点击音效
- 如果你改文件名，记得同步修改 `src/config/appConfig.js`
