请根据下面的输入结构，为炼金炉输出统一语义结果。

输入状态：{input_state}
允许属性：{allowed_attrs}

输入 JSON：
{inputs_json}

请注意：
- 只有至少一个输入非空时才会调用你；不要假装存在缺失的输入。
- text 输入必须参与 `attrSet` 推断，不能视为空属性。
- spell 输入已有的 `attrSet`、名字、`themeText` 都是语义证据。
- `themeText` 不是最终视频 prompt，而是交给下游 prompt 路由器的上游主题文本。
- 如果只有一个输入，就围绕这个输入做重构与延展，不要硬凑“双输入融合”口吻。

输出必须是严格 JSON，只包含：
- name
- attrSet
- themeText
